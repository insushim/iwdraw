# 아트온 전체 최적화 + 학급 알림 실행 계획 v2 (2026-09-02, 실측 기반 · FULL 교차검증 반영 · 실행은 Opus 세션)

## 0. 실측 베이스라인 (라이브 arton.simssijjang.workers.dev, 빌드 1788241478 = HEAD)
| 페이지 | 콜드 전송 | 요청 | LCP(데스크톱) | Lighthouse 모바일 |
|---|---|---|---|---|
| / | 745KB | 43 | 384ms | 67점 · FCP 4.3s · LCP 6.5s |
| /draw | 1392KB | 52 | 812ms · 캔버스 준비 990ms | 68점 · TBT 1050ms |
| /coloring | 6047KB | 119 | 300ms(첫 화면 원본 64장 5MB) | 59점 · LCP 14.0s |
CPU 프로파일(/draw, 4x): JS self ≈ 0.8s 중 단일 최대 = `useAutoHeaderLabels` measure **330ms**(Editor.tsx:120~150, useLayoutEffect 의존성 없음 → 매 렌더 dataset 쓰기+scrollWidth 읽기 ≤3회 = 강제 레이아웃). React 커밋 7회, DOM 394 — 렌더 폭주 없음.
측정 도구(스크래치, 재사용): perf-measure.mjs(페이지 지표, 콜드/SW웜, CPU 스로틀), profile-draw.mjs(CDP 프로파일+React 커밋 수), Lighthouse(lh/).

### 실행 결과 (2026-09-04, 라이브 arton.simssijjang.workers.dev · 빌드 8dee5583)
전송 바이트는 **압축 전송량**(PerformanceResourceTiming.transferSize) 기준.

| 페이지 | 콜드 전송 | 요청 | DOM | img |
|---|---|---|---|---|
| / | 745KB → **657KB** | 43 → 35 | — | — |
| /draw | 1392KB → **749KB** | 52 → 49 | 398 | 16 |
| /coloring | 6047KB → **870KB** | 119 → 64 | 10862 → **346** | 1344 → **30** |

- SW 프리캐시: **137.6MB → 1.74MB**(69항목). ⚠️ §0 의 "6.1MB" 는 `_next/static` 만 센 값이었다 —
  실제 매니페스트는 1958항목·137.6MB 였고 그중 색칠 도안이 130.7MB 였다. 처음 들어온 아이의
  기기가 배경에서 130MB 를 받고 있었다는 뜻이다.
- 해시 청크 `cache-control: public, max-age=31536000, immutable` 라이브 curl 확인.
  해시 없는 경로(version.json·manifest·sw.js·HTML)는 `max-age=0, must-revalidate` 유지.
- /draw CSS 참조 7개 → 2개(out/draw.html 실측).
- 도구 아이콘 634KB → 45KB, Pretendard 차단 시 첫 콘텐츠 30초+ 정지 → 45ms.
- 라이브 회귀 141 통과. 유일한 실패 `acrylic-pen` 은 **기존 결함** — 기준선 커밋(6c99883)의
  프로덕션 빌드도 같은 26(임계 24)이 나온다. dev 서버에서만 통과하던 경계값 스펙이다.

**성공 조건 대비**: S1 달성 · S2 부분(1392→749KB, 목표 ≤650KB) · S3 부분(immutable 달성,
프리캐시 1.74MB vs 목표 0.9MB — 단 진짜 기준선은 137.6MB) · S4 달성(신규 회귀 0) ·
S5 부분(안내·입장·경로 복귀 확인. "입장 후 복구 배너" E2E 는 만들 수 없다 — 복구는 래스터라
획 로그가 없어 무비/복구 조합을 스펙으로 재현할 수 없다).

### 계획에 없던 발견 (실행 중 확정)
1. **SW 프리캐시 137.6MB**(위) — §0 이 크게 과소평가.
2. **토스트가 캔버스 탭을 삼킴** — 안내가 뜬 6초 동안 하단 중앙의 탭·획이 통째로 먹혔다.
   학급 안내를 넣으며 드러났지만 저장 토스트가 예전부터 같은 자리를 막고 있던 기존 결함.
3. **지연 로딩이 만든 두 타이밍 결함**(배포 후 라이브에서만 재현) — 헤더가 리렌더 없이
   커져 [저장하기]가 화면 밖으로 밀림 / 무비가 빈 화면. 원인·처방은 커밋 f50e482.
4. **원본이 0바이트인 도안 1장**(fruits_veggies_mid_13) — 갤러리에 깨진 카드로 보인다. 미해결.
5. **B1 의 전제 오류** — `public/_headers` 는 gitignore 에 없고 추적되고 있었다(잠복 결함 아님).
6. **B7 은 이미 절반 구현돼 있었다** — 삭제 자체는 있었고, 없던 것은 created_at 인덱스와 배치.

## 1. 성공 조건 (배포 후 같은 도구로 재측정)
- S1 /coloring 콜드 전송 6.0MB → **≤1.0MB**, DOM 요소 수 1344항목 → ≤120, 모바일 LCP 14s → ≤4s
- S2 /draw 콜드 전송 1.39MB → ≤0.65MB, 프로파일에서 헤더 measure self ≤20ms(330→), CSS 파일 7 → 2
- S3 해시 청크 `cache-control: immutable`(curl 확인), SW 프리캐시 6.1MB → ≤0.9MB(폰트 조각 546개 제외)
- S4 라이브 회귀: `npx playwright test --config=playwright.live.config.ts --project=desktop-chrome` 전부 통과(제목 스펙 제외 규약 유지)
- S5 게스트가 /draw 에서 "학급 코드로 입장하면 우리 반 갤러리에 전시할 수 있어요" 알림을 보고 → 입장 후 /draw 로 복귀, 그리던 그림 복구 배너 동작(E2E 1개 추가)

## 2. 작업 항목 (우선순위 순 · 각각 독립 커밋)
### A. 학급 알림(기능, 사용자 요청) — 1~2파일
- 조건: `hasBackend() && !onSave && !room`(학생 세션 없음 + 혼자 그리기). 협동방은 제외(모둠 캔버스는 갤러리 제출 흐름이 다르고 헤더가 이미 가득 — Claude 렌즈). 교사 미리보기는 클라이언트에서 구분할 신호가 없어 그대로 노출(문구는 사실이므로 허용, 추후 교사 세션 감지가 생기면 제외).
- UI ①진입 시 세션당 1회 토스트(6초, sessionStorage `arton.classHint`): 「🏫 학급 코드로 입장하면 그림을 **우리 반 갤러리**에 전시할 수 있어요 [입장하기]」 ②헤더 `hdr-extra` 칩 「🏫 학급 입장」(hdr-label 접기 규칙 적용) ③「내 기기에 저장」 성공 토스트 뒤 같은 힌트 1회.
- 링크 = `/join?next=<현재 경로+쿼리>`. join-client `enter()` 성공 시 `next` 검증 = `new URL(next, location.origin).origin === location.origin` **그리고** `next` 가 `/`로 시작하며 `\\`·제어문자·`//` 를 포함하지 않을 때만 `router.push(next)`, 아니면 `/draw`(교차검증: `/\\evil.example` 은 startsWith 검사만으론 통과 후 브라우저 정규화로 외부 이동). /join 페이지는 이미 `<Suspense>` 로 감싸져 있어 정적 export 무관. **그림 보존은 기존 자동저장(IndexedDB)+RestoreBanner 에 의존** — 입장 후 재마운트 시 복구 배너가 뜨는지 E2E 로 확인(뜨지 않으면 이 항목 미완).
- 세션 키 수명주기: ①은 `arton.classHint.entry`, ③은 **별도 키** `arton.classHint.save`(같은 키면 ③이 영구 휴면 — Grok). 입장 성공(joinClass ok) 시 두 키 모두 삭제 — 공유 웨일북에서 다음 아이가 같은 탭을 써도 알림이 다시 뜬다(codex). 협동방(room)·교사 미리보기도 세션이 없으면 동일하게 표시(교사 미리보기엔 문구가 어색하지 않음 — 「학급 코드로 입장하면」은 사실).
- 파일: Editor.tsx(토스트·칩), draw-client.tsx(변경 없음 예상), join-client.tsx(next), tests/e2e/guest-flows.spec.ts(+1: 게스트 알림→입장→복귀·복구 배너), tests/e2e/header-layout.spec.ts(+1: 게스트 칩 유무에 따른 라벨 접힘 — 헤더 항목 수가 바뀌는 케이스는 과거 xl 기준을 깨뜨린 이력).

### B. 즉시 효과·저위험 (각 30분 이내)
- B1 **캐시 헤더**: `public/_headers` 는 손으로 쓴 정적 파일인데 a0a60f9 커밋이 만들면서 동시에 **gitignore 에 넣어** 버렸다(새 클론이면 보안헤더가 통째로 사라지는 잠복 결함) → `.gitignore` 에서 제거하고 커밋. 여기에 `/_next/static/*` → `Cache-Control: public, max-age=31536000, immutable` 블록 추가(CF Static Assets `_headers` 문법 = Pages 와 동일, `*` 와일드카드). 해시 없는 경로(templates/icons/version.json/manifest/sw.js/HTML)는 **절대 immutable 금지**(기존 max-age=0 유지). 자가치유 루프와의 정합: version.json·HTML 은 그대로 network-first 이므로 영향 없음 — 배포 후 curl 로 두 경로의 헤더를 각각 확인.
- B2 **SW 프리캐시 다이어트** `src/app/sw.ts`: precacheEntries 필터에 **폰트 확장자만** 제외(`/\.(woff2?|ttf|otf)$/`, 디렉터리 통째 제외 금지 — media 에 비폰트 자산이 생기면 오프라인 복구 불가·codex). 폰트는 런타임 `static-font-assets`(SWR, maxEntries 4 → 커스텀 규칙으로 **64** 로 상향, 한글 조각은 사용 시 여러 개). 커스텀 런타임 규칙은 **전부 defaultCache 앞**에(순서가 곧 우선순위): ① `/api/student/file` NetworkOnly(공유 기기 프라이버시 — 뒤에 두면 defaultCache `apis` 에 가려 휴면·Grok) ② `/templates/_thumbs/…webp` = `template-thumbs` SWR maxEntries **1500** ③ `/templates/…webp` 원본 = `template-images` SWR maxEntries 200(썸네일과 원본을 한 버킷에 두면 C1 이후 다시 스래싱·codex/Grok). activate 핸들러가 비-precache 캐시를 전부 지우므로 기존 `apis` 캐시의 학생 파일도 다음 배포에 소거됨(확인만). 검증 = 배포 후 SW 설치 크기(Cache Storage 합) ≤0.9MB, 오프라인 새로고침 스모크 1회(boot-selfheal.spec 유지).
- B3 **도구 아이콘** `public/icons/tools/*.png` 15장(256px 36~79KB, 20~24px 렌더) → 96px WebP(q85) 재출력(스크립트 1회, sharp), icons.tsx:449 확장자 변경, PNG 삭제(SVG 폴백 경로는 유지). 기대 732KB → ≤60KB. `brush-tips/bristle.png`(79KB)는 **건드리지 않는다** — 브러시 팁 알파맵은 밉맵·재깅 회귀 이력이 있고(메모리 arton-brush-rendering) 1파일 79KB 는 캐시로 흡수된다(Claude 렌즈).
- B4 **헤더 라벨 강제 레이아웃**: `useAutoHeaderLabels` 의 렌더-후 측정에 시그니처 가드 — `el.clientWidth + '|' + el.childElementCount + '|' + el.textContent` 가 직전 측정과 같으면 skip. **ResizeObserver 콜백과 `document.fonts.ready` 는 가드를 우회해 강제 재측정**(리사이즈·웹폰트 도착은 텍스트가 같아도 폭이 변한다 — 3계열 공통 지적). 검증 = profile-draw.mjs 에서 해당 함수 self ≤20ms(330→) + header-layout/header-reachable 스펙 + 창 폭 1366→900 리사이즈 시 라벨 접힘 유지.
- B5 **외부 CSS**: Pretendard CSS 는 렌더 블로킹 + 학교망에서 jsDelivr 가 막히면 **첫 페인트가 타임아웃까지 정지**한다(codex). 처방 = `preconnect` 추가 **+ 비차단 로드**(`media="print" onload="this.media='all'"` 패턴 또는 클라이언트 마운트 후 link 주입) — 폰트 스택에 시스템 한글 폴백이 있어 FOUT 만 발생. 자체 호스팅 서브셋은 C4.
- B6 **랜딩 프리페치**: 「교사 시작하기」·CTA 교사 링크 `prefetch={false}`(35KB, 학생 기기 불필요). /draw·/coloring 프리페치는 유지.
- B7 **join_attempts 무한 증가**(정리 경로 없음, 실측): cron `scheduled` 에 `DELETE FROM join_attempts WHERE created_at < ?` bind `Date.now() - DAY_MS`(컬럼은 **정수 ms**, 의사 SQL `now-1d` 아님 — codex/Gemini). 기존 인덱스는 `(ip_hash, created_at)` 라 created_at 단독 삭제가 풀스캔 → `schema.sql` 에 `join_attempts_created_idx ON join_attempts(created_at)` 추가(멱등, cf:deploy 가 migrate-remote 로 적용) + `WHERE id IN (SELECT id … LIMIT 500)` 배치로 첫 실행 대량 삭제 보호.

### C. 구조 변경 (측정 후 진행)
- C1 **색칠 페이지 썸네일+테마 우선 탐색** `coloring-gallery.tsx`: ①스크립트로 `public/templates/_thumbs/<theme>/<id>.webp`(320px, q80, ≈6~10KB) 1344장 생성 + manifest 에 `thumb` 필드(import-iwart-templates.mjs 의 비-iwart 항목 보존 머지 규칙 유지, gen-masters 산출도 포함) ②그리드는 `thumb` 사용, `decoding="async"`, width/height 명시 ③**기본 화면 = 테마 커버 그리드**(manifest `cover`, 27장) → 테마 클릭 시 그 테마 항목만(≤60) 표시, 학년 필터는 테마 안에서 적용. 「더 보기」증분은 채택하지 않는다 — 증가만 있고 감소가 없어 결국 1344 로 돌아간다(codex/Grok). 마운트 항목 상한 ≤120 을 코드 상수로 고정 ④인쇄·그리기 링크는 원본 경로 유지. 검증 S1 + coloring 관련 e2e(guest-flows 의 도안 선택 흐름은 테마 클릭 1단계가 늘어나므로 스펙 갱신).
- C2 **글씨 글꼴 CSS 분리**: `TextPalette` 를 `next/dynamic`(ssr:false)로 지연 → **실측 확인됨**(임시 워크트리 빌드: out/draw.html CSS 참조 7→2, 폰트 CSS 5개는 비동기 청크로 남음 — Gemini 의 "next/font 는 전역이라 분리 불가" 주장은 반증). 짝 맞추기(Grok): 무비 재생 `MovieModal → drawTextOnCtx` 는 글씨 도구를 열지 않은 세션(복구 후 바로 무비)에서 글꼴 CSS 없이 그린다 → `ensureTextFontsLoaded()`(= `await import("@/lib/fonts")` 후 `document.fonts.load`) 를 TextInsert.ensureFontReady 와 MovieModal 재생 직전에 공통 호출. 자동저장 복구는 래스터라 무관. 검증 = text-tool.spec + movie-fidelity.spec 에 「글씨 넣기 → 새로고침 복구 → 무비 재생」 케이스 1개 추가.
- C3 **draw 번들 지연 로딩**: MovieModal(+TimelapseExporter)·CollabStartModal·PhotoImport(photo-to-lineart 42KB)·StampPalette 를 dynamic import. 엔진이 쓰는 StampTool/SketchMatch 는 유지. 기대 draw 청크 299KB → ≤210KB(raw). 검증 = 각 모달 스펙(movie-fidelity, collab-entry, paste-drop-import, pending-stamp).
- C4 Pretendard 자체 호스팅(동적 서브셋 CSS+woff2 를 `public/fonts/`) — B5 의 비차단 로드로 정지는 막지만 CDN 차단 학교망에선 글꼴 자체가 안 온다. C1~C3 후 진행, SW 폰트 규칙(B2)이 그대로 적용된다.

### D. 하지 않을 것 (근거)
- 합성 백킹 축소·부분 합성(메모리 arton-composite-optimizations-rejected: 이 맥 `?perf=1` A/B 에서 둘 다 더 느림 — 수치·조건은 그 파일), 엔진 핫패스 변경(웨일북 실측 없이 금지), 해시 없는 경로 immutable, DeepSeek/로컬 검증.

## 3. 순서·검증·배포
1. 커밋 단위 = A, B1, B2, B3, B4, B5, B6, B7, C1, C2, C3 각각. **각 커밋 직후** `pnpm typecheck` · `pnpm test` · 그 항목의 e2e(로컬) 를 돌리고 통과해야 다음 항목(회귀 시 원인 커밋 즉시 특정 — Claude 렌즈). B 전부 끝난 뒤 로컬 `pnpm cf:build` + `wrangler dev`(NEXT_PUBLIC_HAS_BACKEND=1) 로 perf-measure(BASE=localhost) 전후 비교표 작성, C 도 항목마다 같은 비교.
2. 전체 끝나면 `preflight-gate.sh` → LITE 재검증(diff+주변부, 신규 CRITICAL 시 FULL).
3. 배포: `pnpm cf:deploy`(마이그레이션 없음 확인, wrangler 계정 = naver 4644562c) → `/version.json?cb=` 확인 → 라이브 헤더 curl(S3) → 라이브 전체 회귀(S4) → 라이브 perf-measure/Lighthouse 재측정 표를 이 문서 §0 아래에 추가.
4. 메모리: arton 성능 베이스라인·헤더 measure 함정·SW 프리캐시 폰트 함정 기록.

## 4. 교차검증 원장 (FULL, 2026-09-02 · 계획 문서 대상)
- 참여: Tier-0 게이트 = N/A(문서) · codex(gpt-5.6-sol) 12건 · Gemini(3.7 Flash) 6건 · Grok(grok-4.6, 축약 번들 6.3KB 재발사) 6건 · Claude code-reviewer 5건. 병합 스크립트(xval-merge)가 멈춰 메인이 raw JSON 을 직접 대조.
- 채택(계획에 반영): next 오픈 리다이렉트 origin 검증(codex+Gemini) · 알림 키 분리·입장 시 삭제(codex+Grok+Claude) · B2 폰트 확장자 제외·규칙 순서·썸네일/원본 캐시 분리(codex+Grok) · B4 clientWidth+RO/fonts 우회(codex+Gemini+Grok) · B5 비차단 로드 승격(codex) · B7 정수 ms 바인드+created_at 인덱스+배치(codex+Gemini) · C1 테마 우선·증분 폐기(codex+Grok) · C2 무비 재생 글꼴 짝(Grok) · A 협동방 제외·헤더 스펙(Claude) · B3 bristle 제외(Claude) · 커밋별 게이트(Claude) · B1 `_headers` gitignore 잠복 결함(메인 실측).
- 기각(근거): Gemini "join 페이지 Suspense 필요" — 이미 `src/app/join/page.tsx` 가 Suspense 로 감쌈. Gemini "next/font 는 전역이라 dynamic import 로 분리 불가" — 임시 워크트리 빌드 실측으로 반증(draw.html CSS 7→2). Gemini "`/templates/…webp`(정규식 matcher) glob 문법 오류" — 계획 표기일 뿐, 구현은 정규식/함수 matcher 로 쓴다(표기 수정). codex "SW 갱신·오프라인 E2E 게이트 추가" — 기존 boot-selfheal.spec 유지 + 스모크 1회로 갈음(범위 초과). codex "A/B 수치 부재" — 메모리 파일 참조로 해결.
